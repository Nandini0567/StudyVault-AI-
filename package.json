{
  "name": "studyvault-ai",
  "version": "1.0.0",
  "private": true,
  "description": "StudyVault AI - Personal Academic Knowledge Vault",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "backend": "backend\\venv\\Scripts\\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --app-dir backend",
    "start": "npm run dev"
  }
}
